<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.rm.mapper.AttachMapper">
	<select id="getAttachList" resultType="com.rm.model.AttachImageVO">
	
		select * from room_image where roomcode = #{roomcode}
	
	</select>
  </mapper>